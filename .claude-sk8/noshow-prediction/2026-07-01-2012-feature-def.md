# Feature: Predicción de No-show de turnos médicos (implementación Python)
Fecha: 2026-07-01
Status: Approved
Feature Branch: `feat/noshow-prediction`
Base Branch: `main`
OpenSpec Change: `openspec/changes/noshow-prediction/`

## Business Context
**Problem Statement**: El ausentismo (no-show) es ~20 % de los turnos (20,2 % en el dataset: 22.319/110.527). Cada ausencia = franja del profesional ociosa + un paciente de la lista de espera que no pudo atenderse. Hoy no hay predicción anticipada → no se puede accionar.
**Target Users**: Gerencia Comercial y Técnica (audiencia del TPO). Usuario de la app: gestor de agenda del centro de salud.
**Success Metrics**: Modelo con buen recall/F1/AUC sobre la clase "falta"; app funcional que traduce probabilidad en acción; coherencia hipótesis↔conclusión (criterio de evaluación de la cátedra).
**Priority**: Entregable obligatorio del TPO.

## Alcance / Decisiones
| Tema | Decisión |
|---|---|
| App interactiva | Streamlit (individual + lote) |
| 2.ª fuente (superadora) | Clima INMET 2016, estación A612 (Vitória, ES) |
| Datos | `no-show-dataset.csv` (10,7 MB) y `weather-dataset.csv` (437 MB) ya en el repo |
| Idioma | Docs/EDA/app español, código inglés (a confirmar) |
| Modelos | Decision Tree + Random Forest, `class_weight="balanced"` |
| Métricas | precision, recall, F1, ROC-AUC, PR-AUC (accuracy evitada por desbalance) |

## Fuentes de datos (verificado)
- Turnos: 110.527 filas, 14 columnas, target `No-show`→`no_show`; 20,2 % faltó. `AppointmentDay` 2016-04-29→2016-06-08; `ScheduledDay` 2015-11-10→2016-06-08.
- Clima: INMET horario 2016, 4,37M filas, 529 estaciones; se usa A612 (Vitória, validada costera). Se agrega horario→diario y se cruza por `AppointmentDay`.

## Requisitos y escenarios
Formalizados en OpenSpec (GIVEN/WHEN/THEN) por dominio:
- `data-pipeline` — ingesta+validación, normalización target, limpieza, feature engineering.
- `weather-integration` — filtrado A612, agregación horario→diario, join por fecha.
- `noshow-modeling` — preprocesado sin fuga, entrenamiento DT/RF estratificado, probabilidad por turno, evaluación.
- `interactive-app` — predicción individual, recomendación de acción por riesgo, modo lote + valor de negocio.

## Edge cases / cuidados
- Edad negativa (-1) y lead time negativo → limpiar con conteo.
- `Neighbourhood` alta cardinalidad (~81) → agrupar raros antes de OHE.
- `SMS_received` confundido con lead time → NO interpretar como causal; documentar.
- Fechas de turno sin clima → imputación/marca sin romper pipeline.
- Sin fuga: `ColumnTransformer` ajustado solo en train, persistido con el modelo.

## Riesgos
- Python 3.14 sin wheels → fallback venv 3.12.
- Clima 437 MB → lectura por chunks + caché diario; crudos grandes en `.gitignore`.
- Datos 2016 / una ciudad / pre-COVID → sección de limitaciones + mención de concept drift.

## Implementation Plan (atomic changes)
Ver `openspec/changes/noshow-prediction/tasks.md` (grupos 1–7). Un branch de feature `feat/noshow-prediction` + commits convencionales por módulo (ceremonia sk8 simplificada para TP académico).

## Next Steps
- `/sk8:build` para ejecutar las tareas de implementación.
- `/sk8:specs` para navegar las specs.
- `/sk8:verify` tras implementar, para chequear cumplimiento de escenarios.

## Referencia
- Plan aprobado: `/Users/fmartinezvidal/.claude/plans/serene-gathering-meadow.md`
- Consigna: `consignas.pdf` (desarrollo Grupo 7), `tpo.pdf` (cátedra).
