# Resumen de ejecución /sk8:build — noshow-prediction

Branch `feat/noshow-prediction` (pusheada). Modo spec-driven. **33/33 tareas completas.**

## Entregado
- **Capa de datos** (`src/noshow/`): config, data_load (validación esquema + target), weather (INMET A612 por chunks → diario), features (lead_time, grupos etarios, comorbilidades, barrios, clima).
- **Modelado**: preprocess (OHE, sin escalado), train (DT podado + RF, hold-out estratificado + StratifiedKFold), evaluate (classification_report, ConfusionMatrixDisplay, ROC-AUC, feature_importances, plot_tree, threshold_analysis), predict.
- **App Streamlit** (`app/`): turno individual + agenda lote, banda de riesgo + acción, valor de negocio. Reusa el pipeline persistido (sin fuga).
- **Notebook CRISP-DM** (50 celdas, ejecutado sin errores), `docs/architecture.md` (Mermaid), `README.md`, 12 figuras.
- **Packaging**: pyproject.toml + `pip install -e .`.

## Resultados reales
- Turnos: 110.527 → 110.521 tras limpieza; no-show 20,2%.
- Clima A612 Vitória: 366 días; weather_missing=0.
- **RF elegido** (ROC-AUC test 0,733). A umbral de negocio recall≈0,72 / F1≈0,45.
- Bandas recalibradas a la distribución real: RISK_LOW=0,28 (p66), RISK_HIGH=0,345 (p90) → bajo 66% / medio 24% / alto 10%.
- Hallazgo: lead_time + same_day ≈ 62% de la importancia; comorbilidades <2%.

## Verificación end-to-end
- **66 tests** pytest verdes (fixtures sintéticas, no leen datos reales).
- 7.1 data_load valida esquema ✓ · 7.2 train → model.joblib + metrics.json ✓ · 7.3 streamlit health ok ✓ · 7.4 notebook 26 celdas código, 0 errores ✓.

## Decisiones registradas
- Selección de modelo por ROC-AUC de CV (F1 a 0.5 degenera por desbalance sin class_weight; alineado a "umbral orientado a recall").
- Bandas de riesgo priorizan objetivo poblacional (~34% acción, ~10% agresiva) sobre anécdotas; el turno "68/hipertensa/lead30" es promedio para el modelo.
- Ceremonia sk8 simplificada: commits convencionales por fase sobre la feature branch (TP académico), no sub-branches/PRs atómicos.

## Commits (7)
define+alineación cátedra → capa datos → modelado → app → notebook/docs → recalibración → packaging.

## Próximo
- `/sk8:verify` para chequear cobertura de escenarios GIVEN/WHEN/THEN.
- PR `feat/noshow-prediction` → `main` cuando quieras (push a main está bloqueado por política; se hace vía PR).
