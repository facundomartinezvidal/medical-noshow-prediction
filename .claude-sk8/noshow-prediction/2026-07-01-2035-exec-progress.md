# Ejecución /sk8:build — noshow-prediction

Branch: `feat/noshow-prediction` · Modo: spec-driven · Sin Linear binding.

## Entorno
- venv `.venv` con Python **3.13** (3.14 no probado; 3.13 tiene wheels). pandas 3.0.3, sklearn 1.9.0, numpy 2.5.0, streamlit + viz + pytest.
- Datos crudos movidos a `data/raw/` (gitignoreados). `requirements.txt` freezeado.

## Fases (delegadas a agentes; el PM valida y commitea)
- [~] **Fase 1 — Capa de datos**: config, data_load, weather (A612 chunked), features + tests. → @sk8:developer (en curso)
- [ ] **Fase 2 — Modelado**: preprocess (ColumnTransformer OHE), train (DT podado + RF, StratifiedKFold), evaluate (classification_report + ConfusionMatrixDisplay + ROC-AUC), predict + tests.
- [ ] **Fase 3 — App Streamlit**: individual + lote + acción por riesgo + valor.
- [ ] **Fase 4 — Notebook (CRISP-DM) + docs (arquitectura Mermaid) + README**.

## Decisiones de ejecución
- Ceremonia sk8 de sub-branches atómicos simplificada (TP académico): commits convencionales por fase sobre la feature branch.
- Tests: fixtures sintéticas chicas; prohibido cargar 437 MB / 110k filas en unit tests. Smoke test real aparte.
